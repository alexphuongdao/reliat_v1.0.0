# Website video assets

Only approved, web-optimized media loaded by the landing page belongs here.
Keep raw generations and editing masters in `production/hero-mine/`.

Final filenames will distinguish the desktop and mobile compositions, for
example `mine-hero-desktop.mp4` and `mine-hero-mobile.mp4`.
